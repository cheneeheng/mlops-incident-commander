# Decision Log

### Entry 1

**Type:** Decision
**Mode:** Autonomous
**Timestamp:** 2026-07-23
**Task:** Implement v1 plan family (SKELETON..ITER_05), foundation design decisions.

**Context:** Three forks the plan/invariants left underspecified:
1. Public ID scheme — architecture invariant mandates app-generated prefixed URL-safe IDs
   (`{prefix}_{token_urlsafe(12)}`) and forbids exposing auto-increment; but the plan (ITER_02 §02)
   says "Incident IDs via DB sequence (concurrency gotcha)".
2. Layering — architecture invariant requires routers -> services -> db with no business logic in
   routers; the plan's §04 file tree lists `routers/` and `db/` but no `services/`.
3. `implementation-gotchas.md` — §04/§05 tell me to read it, but no such file exists in the repo.

**Decision:**
1. Every entity gets a prefixed-token string PK (invariant, non-negotiable). Incident additionally
   gets a `number` int column backed by a Postgres SEQUENCE for human-facing display — this honors
   the plan's "DB sequence, not MAX+1" concurrency note without exposing an int as the public ID.
2. Add a `services/` layer. The invariant is non-negotiable ("route handlers contain no business
   logic"). Routers stay thin and call services; services hold logic and call the db layer.
   This is an invariant-required addition to the plan's tree, not scope creep.
3. Do not author a separate gotchas doc. Every gotcha the plan references is described inline in the
   plan docs; I apply each in code with a `# gotcha:` comment naming it.

**Impact / Risk:** Slightly larger surface than the literal plan tree (services layer, incident
`number` column). Low risk; both are mandated by the project's non-negotiable invariants and keep
the code consistent across 13 entities.

**Outcome:** Applied from SKELETON onward.

### Entry 2

**Type:** Decision
**Mode:** Autonomous
**Timestamp:** 2026-07-23
**Task:** CNN weights for the serving classifier.

**Context:** SKELETON §03 says "CIFAR-10 weights checked in" but no weights exist in the repo, and
training a real CIFAR-10 CNN is out of scope for a plan-implementation pass and needs a GPU/dataset.

**Decision:** Provide a small `scripts/seed_weights.py` that constructs the CNN and writes two
checkpoints: `v1.0-good` (deterministic init) and `v1.1-bad` (deliberately degraded — weights
perturbed/zeroed so accuracy and confidence drop). The app requires a working good/bad CNN pair for
the bad-deploy fault, not high accuracy. Weights are generated by the script, not committed.

**Impact / Risk:** Predictions are not meaningfully accurate until real training is done; acceptable
because the system measures drift/latency/distribution shifts, not classifier quality. Documented as
a manual follow-up.

**Outcome:** Applied in ITER_01.
