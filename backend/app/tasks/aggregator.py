"""Metrics aggregator: 30s windows over prediction_log. Stub at SKELETON; real computation
(percentiles, entropy, PSI vs reference profile) lands in ITER_01, and posting windows into the
agent graph lands in ITER_02."""

import asyncio


async def run_aggregator() -> None:
    # stub — real implementation in ITER_01
    await asyncio.sleep(0)
