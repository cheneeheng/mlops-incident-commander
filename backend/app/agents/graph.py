"""LangGraph agent graph. Stub at SKELETON — the monitor/diagnosis/second-opinion/adjudicate/
remediate/postmortem nodes are wired across ITER_02 (monitor+diagnosis), ITER_03 (remediation),
and ITER_04 (second-opinion, adjudicator, postmortem)."""


async def process_window(window_id: str) -> None:
    """Entry point: the aggregator posts each new metric window here for the monitor node to triage.

    Stub at SKELETON — implemented in ITER_02.
    """
    return None
